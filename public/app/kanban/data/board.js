define([

    'app',

    'angular'

], function(app, angular) {
    "use strict";

    app.value('dataBoard', [
        {
            "id": "0",
            "title": "Main",
            "columns": ["0", "1"]
        }
    ]);
});